/// @description Insert description here
// You can write your code in this editor

image_alpha = 0;

x_init = x + 57;

y += 15;

rotate = 0;

xmod = 0;
ymod = 0;

label = scribble(text);
label.starting_format("ft_gui", c_white);
label.origin(0, 15);
label.wrap(114)

windowScale = global.window_scale;